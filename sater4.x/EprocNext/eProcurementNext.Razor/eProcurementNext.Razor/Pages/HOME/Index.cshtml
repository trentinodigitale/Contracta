@page "/HOME/"
@model eProcurementNext.Razor.Pages.HOME.IndexModel
@inject eProcurementNext.Application.IEprocNextApplication application;
@{
    // pagina usata al momento solo per test

    Response.Redirect("/");
    //Response.Redirect(application["WEBSERVERPORTALE"] + "/" + application["NOMEAPPPORTALE"] + "/index.asp");
}