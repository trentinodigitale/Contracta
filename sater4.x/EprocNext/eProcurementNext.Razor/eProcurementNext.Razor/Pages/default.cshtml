@page "/default.asp"
@using System.Web;
@using eProcurementNext.CommonModule;
@model eProcurementNext.Razor.Pages.defaultModel
@using static eProcurementNext.Session.SessionMiddleware;
@inject eProcurementNext.Application.IEprocNextApplication application;
@{
	//Response.Redirect(application["WEBSERVERPORTALE"] + "/" + application["NOMEAPPPORTALE_JOOMLA"] + "/");
	throw new ResponseRedirectException($"{DefaultHomePage}", Response);
}